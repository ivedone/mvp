import 'package:flutter/material.dart';
import 'package:mvp/theme/colors.dart';

final ThemeData activeTheme = ThemeData(
  colorScheme: activeColorScheme,
);

final ThemeData getReadyTheme = ThemeData(
  colorScheme: getReadyColorScheme,
);

final ThemeData restTheme = ThemeData(
  colorScheme: restColorScheme,
);
